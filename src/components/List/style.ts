import { clsx } from 'clsx';

export const style = {
  headerContainer: clsx('gap-4'),
  emptyContainer: clsx('justify-center items-center h-[300] gap-4'),
  emptyContainerText: clsx('text-2xl color-mutedSlate font-regular'),
};
